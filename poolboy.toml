spec = "0.2"
name = "Poolboy"
corpus = "docs"
output = "dist"

[[render]]
template = "templates/commands.md.knap"
data = "data/commands.json"
output = "reference/commands.md"

# The published docs landing uses built-in defaults. To customize it, copy
# landing.example.toml to landing.toml and edit the values you want; any option
# you leave out keeps its default. Delete landing.toml to return to defaults.
